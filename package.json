{
  "name": "slime-orb",
  "version": "0.1.0",
  "private": true,
  "description": "A voice agent rendered as a slime, driven by an OpenAI Realtime call",
  "type": "module",
  "engines": {
    "node": ">=22.12"
  },
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "start": "node --env-file-if-exists=.env src/server/index.js",
    "preview": "npm run build && npm start",
    "test": "node --test",
    "lint": "eslint ."
  },
  "dependencies": {
    "three": "^0.185.1"
  },
  "devDependencies": {
    "@eslint/js": "^10.0.1",
    "eslint": "^10.8.0",
    "globals": "^17.8.0",
    "vite": "^8.1.5"
  }
}
